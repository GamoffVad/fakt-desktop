<!--
    FAKT · Arctic Blue · inputs (DESIGN.md §6.2).

    Field anatomy (TextBox, PasswordBox, ComboBox):
        Bd (Surface, Border.Input, radius 6) < HoverBorder (Action.Primary, fades 120 ms)
        < ErrorBorder (Red.700 when ui:Ui.HasError or Validation.HasError) < FocusRing.
    FocusRing: 2 DIP Brush.Focus frame, 2 DIP outside the field (no size change), shown while the
    field has keyboard focus - it stays visible on top of the error state.
    Read-only: Background.Subtle, text stays selectable/copyable. Disabled: Disabled.* colours,
    no hover. The permanent label above a field is the view's job (Text.FieldLabel);
    ui:Ui.Placeholder is only an optional hint.
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:Fakt.Desktop.Controls">

    <!-- ===================== TextBox ===================== -->

    <ControlTemplate x:Key="TextBox.Template" TargetType="{x:Type TextBox}">
        <Grid x:Name="Root" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="HoverBorder"
                    Opacity="0"
                    BorderBrush="{DynamicResource Brush.Action.Primary}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="ErrorBorder"
                    Visibility="Collapsed"
                    BorderBrush="{DynamicResource Brush.Status.Error}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>

            <Grid x:Name="ContentArea" Margin="{TemplateBinding Padding}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PlaceholderText"
                           Margin="2,0,0,0"
                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                           Foreground="{DynamicResource Brush.Text.Secondary}"
                           Text="{Binding Path=(ui:Ui.Placeholder), RelativeSource={RelativeSource TemplatedParent}}"
                           TextTrimming="CharacterEllipsis"
                           IsHitTestVisible="False"
                           Visibility="Collapsed"/>
                <ScrollViewer x:Name="PART_ContentHost"
                              Focusable="False"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
                              VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"/>
                <ui:Icon x:Name="ErrorGlyph"
                         Grid.Column="1"
                         Margin="8,0,0,0"
                         VerticalAlignment="Center"
                         Size="{StaticResource Size.Icon.Small}"
                         Data="{StaticResource Icon.Error}"
                         Foreground="{DynamicResource Brush.Status.Error}"
                         Visibility="Collapsed"/>
            </Grid>

            <Border x:Name="FocusRing"
                    Margin="{StaticResource Thickness.Focus.Outset}"
                    BorderThickness="{StaticResource Thickness.Focus}"
                    CornerRadius="{StaticResource Radius.Focus}"
                    BorderBrush="{DynamicResource Brush.Focus}"
                    IsHitTestVisible="False"
                    Visibility="Collapsed"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="Text" Value="{x:Null}">
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsEnabled" Value="True"/>
                    <Condition Property="IsReadOnly" Value="False"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
            <Trigger Property="IsReadOnly" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Background.Subtle}"/>
            </Trigger>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Visible"/>
                <Setter TargetName="ErrorGlyph" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="ui:Ui.HasError" Value="True">
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Visible"/>
                <Setter TargetName="ErrorGlyph" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="FocusRing" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="ErrorGlyph" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="PlaceholderText" Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="TextBox.Default" TargetType="{x:Type TextBox}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
        <Setter Property="AllowDrop" Value="True"/>
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Background.Surface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Input}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource Brush.Action.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Thickness.Border}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}"/>
        <Setter Property="MinHeight" Value="{StaticResource Size.Control}"/>
        <Setter Property="Padding" Value="{StaticResource Thickness.Input.Padding}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template" Value="{StaticResource TextBox.Template}"/>
    </Style>
    <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Default}"/>

    <!-- 32 DIP field inside table rows (mapping grid). -->
    <Style x:Key="TextBox.Compact" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Default}">
        <Setter Property="MinHeight" Value="{StaticResource Size.Control.Compact}"/>
        <Setter Property="Padding" Value="8,0"/>
    </Style>

    <!-- Multi-line text (headers, JSON, notes). -->
    <Style x:Key="TextBox.Multiline" TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Default}">
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="MinHeight" Value="80"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
    </Style>

    <!-- ===================== PasswordBox ===================== -->

    <ControlTemplate x:Key="PasswordBox.Template" TargetType="{x:Type PasswordBox}">
        <Grid x:Name="Root" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="HoverBorder"
                    Opacity="0"
                    BorderBrush="{DynamicResource Brush.Action.Primary}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="ErrorBorder"
                    Visibility="Collapsed"
                    BorderBrush="{DynamicResource Brush.Status.Error}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Grid Margin="{TemplateBinding Padding}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ScrollViewer x:Name="PART_ContentHost"
                              Focusable="False"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              HorizontalScrollBarVisibility="Hidden"
                              VerticalScrollBarVisibility="Hidden"/>
                <ui:Icon x:Name="ErrorGlyph"
                         Grid.Column="1"
                         Margin="8,0,0,0"
                         VerticalAlignment="Center"
                         Size="{StaticResource Size.Icon.Small}"
                         Data="{StaticResource Icon.Error}"
                         Foreground="{DynamicResource Brush.Status.Error}"
                         Visibility="Collapsed"/>
            </Grid>
            <Border x:Name="FocusRing"
                    Margin="{StaticResource Thickness.Focus.Outset}"
                    BorderThickness="{StaticResource Thickness.Focus}"
                    CornerRadius="{StaticResource Radius.Focus}"
                    BorderBrush="{DynamicResource Brush.Focus}"
                    IsHitTestVisible="False"
                    Visibility="Collapsed"/>
        </Grid>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsEnabled" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Visible"/>
                <Setter TargetName="ErrorGlyph" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="ui:Ui.HasError" Value="True">
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Visible"/>
                <Setter TargetName="ErrorGlyph" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="FocusRing" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="ErrorGlyph" Property="Visibility" Value="Collapsed"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="PasswordBox.Default" TargetType="{x:Type PasswordBox}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
        <Setter Property="AllowDrop" Value="True"/>
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Background.Surface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Input}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource Brush.Action.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Thickness.Border}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}"/>
        <Setter Property="MinHeight" Value="{StaticResource Size.Control}"/>
        <Setter Property="Padding" Value="{StaticResource Thickness.Input.Padding}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template" Value="{StaticResource PasswordBox.Template}"/>
    </Style>
    <Style TargetType="{x:Type PasswordBox}" BasedOn="{StaticResource PasswordBox.Default}"/>

    <!-- ===================== ComboBox ===================== -->

    <!-- Invisible hit area that opens the drop-down (whole field, or the arrow zone when editable). -->
    <Style x:Key="ComboBox.ToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Chrome-less text part of an editable ComboBox. -->
    <Style x:Key="ComboBox.EditableTextBox" TargetType="{x:Type TextBox}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="AllowDrop" Value="True"/>
        <Setter Property="MinWidth" Value="0"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource Brush.Action.Primary}"/>
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <ScrollViewer x:Name="PART_ContentHost"
                                  Background="Transparent"
                                  Focusable="False"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="ComboBox.Template" TargetType="{x:Type ComboBox}">
        <Grid x:Name="Root" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="Bd"
                    Grid.ColumnSpan="2"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="HoverBorder"
                    Grid.ColumnSpan="2"
                    Opacity="0"
                    BorderBrush="{DynamicResource Brush.Action.Primary}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="ErrorBorder"
                    Grid.ColumnSpan="2"
                    Visibility="Collapsed"
                    BorderBrush="{DynamicResource Brush.Status.Error}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{StaticResource Radius.Control}"/>

            <ContentPresenter x:Name="ContentSite"
                              Margin="{TemplateBinding Padding}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                              IsHitTestVisible="False"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                <ContentPresenter.Resources>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                    </Style>
                </ContentPresenter.Resources>
            </ContentPresenter>
            <TextBlock x:Name="PlaceholderText"
                       Margin="{TemplateBinding Padding}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource Brush.Text.Secondary}"
                       Text="{Binding Path=(ui:Ui.Placeholder), RelativeSource={RelativeSource TemplatedParent}}"
                       TextTrimming="CharacterEllipsis"
                       IsHitTestVisible="False"
                       Visibility="Collapsed"/>
            <TextBox x:Name="PART_EditableTextBox"
                     Margin="{TemplateBinding Padding}"
                     VerticalAlignment="Center"
                     HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                     VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                     Style="{StaticResource ComboBox.EditableTextBox}"
                     IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}"
                     Visibility="Collapsed"/>
            <ToggleButton x:Name="Toggle"
                          Grid.ColumnSpan="2"
                          Style="{StaticResource ComboBox.ToggleButton}"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
            <ui:Icon x:Name="Arrow"
                     Grid.Column="1"
                     Margin="8,0,12,0"
                     Size="{StaticResource Size.Icon.Small}"
                     Data="{StaticResource Icon.ChevronDown}"
                     Foreground="{DynamicResource Brush.Text.Primary}"
                     IsHitTestVisible="False"/>
            <Border x:Name="FocusRing"
                    Grid.ColumnSpan="2"
                    Margin="{StaticResource Thickness.Focus.Outset}"
                    BorderThickness="{StaticResource Thickness.Focus}"
                    CornerRadius="{StaticResource Radius.Focus}"
                    BorderBrush="{DynamicResource Brush.Focus}"
                    IsHitTestVisible="False"
                    Visibility="Collapsed"/>

            <Popup x:Name="PART_Popup"
                   Grid.ColumnSpan="2"
                   AllowsTransparency="True"
                   Placement="Bottom"
                   VerticalOffset="4"
                   Focusable="False"
                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                   IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                <Border x:Name="DropDownBorder"
                        MinWidth="{Binding ActualWidth, ElementName=Root}"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                        Background="{DynamicResource Brush.Background.Surface}"
                        BorderBrush="{DynamicResource Brush.Border.Subtle}"
                        BorderThickness="{StaticResource Thickness.Border}"
                        CornerRadius="{StaticResource Radius.Control}"
                        Padding="4"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                    <ScrollViewer x:Name="DropDownScrollViewer" Focusable="False">
                        <ItemsPresenter x:Name="ItemsPresenter"
                                        KeyboardNavigation.DirectionalNavigation="Contained"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="False">
                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="{StaticResource Size.Dropdown.Item}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="True">
                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
            </Trigger>
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter TargetName="Arrow" Property="Data" Value="{StaticResource Icon.ChevronUp}"/>
            </Trigger>
            <Trigger Property="IsEditable" Value="True">
                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                <Setter TargetName="Toggle" Property="Grid.Column" Value="1"/>
                <Setter TargetName="Toggle" Property="Grid.ColumnSpan" Value="1"/>
                <Setter Property="IsTabStop" Value="False"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsEditable" Value="False"/>
                    <Condition Property="SelectedItem" Value="{x:Null}"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsEditable" Value="True"/>
                    <Condition Property="Text" Value=""/>
                </MultiTrigger.Conditions>
                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsEnabled" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBorder" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="ui:Ui.HasError" Value="True">
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter TargetName="FocusRing" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="ErrorBorder" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="Arrow" Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter TargetName="PlaceholderText" Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ComboBox.Default" TargetType="{x:Type ComboBox}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Background.Surface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Input}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Thickness.Border}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}"/>
        <Setter Property="MinHeight" Value="{StaticResource Size.Control}"/>
        <Setter Property="Padding" Value="{StaticResource Thickness.Input.Padding}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template" Value="{StaticResource ComboBox.Template}"/>
    </Style>
    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource ComboBox.Default}"/>

    <!-- 32 DIP selector inside table rows («Поле в таблице»). -->
    <Style x:Key="ComboBox.Compact" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource ComboBox.Default}">
        <Setter Property="MinHeight" Value="{StaticResource Size.Control.Compact}"/>
        <Setter Property="Padding" Value="8,0"/>
    </Style>

    <!-- ===================== ComboBoxItem ===================== -->

    <Style x:Key="ComboBoxItem.Default" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FocusVisual.Inner}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="MinHeight" Value="{StaticResource Size.Dropdown.Item}"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource Radius.Small}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <ContentPresenter.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Background.Navigation}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Selection}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource ComboBoxItem.Default}"/>

    <!-- ===================== CheckBox ===================== -->

    <ControlTemplate x:Key="CheckBox.Template" TargetType="{x:Type CheckBox}">
        <Grid x:Name="Root" Background="Transparent" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="BoxHost"
                  Width="{StaticResource Size.CheckBox}"
                  Height="{StaticResource Size.CheckBox}"
                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                <Border x:Name="Box"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource Radius.Small}"/>
                <Border x:Name="BoxHover"
                        Opacity="0"
                        BorderBrush="{DynamicResource Brush.Action.Primary}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource Radius.Small}"/>
                <Border x:Name="BoxChecked"
                        Visibility="Collapsed"
                        Background="{DynamicResource Brush.Action.Primary}"
                        BorderBrush="{DynamicResource Brush.Action.Primary}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource Radius.Small}"/>
                <Path x:Name="CheckGlyph"
                      Visibility="Collapsed"
                      Data="M4.5,9.25 L7.5,12.25 L13.5,6.25"
                      Stroke="{DynamicResource Brush.Text.OnAction}"
                      StrokeThickness="2"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"/>
                <Path x:Name="IndeterminateGlyph"
                      Visibility="Collapsed"
                      Data="M5,9 L13,9"
                      Stroke="{DynamicResource Brush.Text.OnAction}"
                      StrokeThickness="2"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"/>
            </Grid>
            <ContentPresenter x:Name="ContentSite"
                              Grid.Column="1"
                              Margin="8,0,0,0"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              RecognizesAccessKey="True"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasContent" Value="False">
                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsEnabled" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="BoxHover" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="BoxHover" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Box" Property="Background" Value="{DynamicResource Brush.Selection}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="BoxChecked" Property="Visibility" Value="Visible"/>
                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="{x:Null}">
                <Setter TargetName="BoxChecked" Property="Visibility" Value="Visible"/>
                <Setter TargetName="IndeterminateGlyph" Property="Visibility" Value="Visible"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="BoxChecked" Property="Background" Value="{DynamicResource Brush.Action.Hover}"/>
                <Setter TargetName="BoxChecked" Property="BorderBrush" Value="{DynamicResource Brush.Action.Hover}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="{x:Null}"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="BoxChecked" Property="Background" Value="{DynamicResource Brush.Action.Hover}"/>
                <Setter TargetName="BoxChecked" Property="BorderBrush" Value="{DynamicResource Brush.Action.Hover}"/>
            </MultiTrigger>
            <Trigger Property="ui:Ui.HasError" Value="True">
                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Status.Error}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Box" Property="Background" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter TargetName="BoxChecked" Property="Background" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="BoxChecked" Property="BorderBrush" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter TargetName="CheckGlyph" Property="Stroke" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter TargetName="IndeterminateGlyph" Property="Stroke" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="CheckBox.Default" TargetType="{x:Type CheckBox}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FocusVisual.Small}"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Background.Surface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Input}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Thickness.Border}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template" Value="{StaticResource CheckBox.Template}"/>
    </Style>
    <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource CheckBox.Default}"/>

    <!-- Box only, centred - DataGrid selection columns (give it AutomationProperties.Name). -->
    <Style x:Key="CheckBox.Cell" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource CheckBox.Default}">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0"/>
    </Style>

    <!-- ===================== RadioButton ===================== -->

    <ControlTemplate x:Key="RadioButton.Template" TargetType="{x:Type RadioButton}">
        <Grid x:Name="Root" Background="Transparent" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="DotHost"
                  Width="{StaticResource Size.CheckBox}"
                  Height="{StaticResource Size.CheckBox}"
                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                <Ellipse x:Name="Ring"
                         Fill="{TemplateBinding Background}"
                         Stroke="{TemplateBinding BorderBrush}"
                         StrokeThickness="1"/>
                <Ellipse x:Name="RingHover"
                         Opacity="0"
                         Stroke="{DynamicResource Brush.Action.Primary}"
                         StrokeThickness="1"/>
                <Ellipse x:Name="CheckedFill"
                         Visibility="Collapsed"
                         Fill="{DynamicResource Brush.Action.Primary}"/>
                <Ellipse x:Name="Dot"
                         Width="8"
                         Height="8"
                         Visibility="Collapsed"
                         Fill="{DynamicResource Brush.Text.OnAction}"/>
            </Grid>
            <ContentPresenter x:Name="ContentSite"
                              Grid.Column="1"
                              Margin="8,0,0,0"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              RecognizesAccessKey="True"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasContent" Value="False">
                <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsEnabled" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="RingHover" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="RingHover" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Ring" Property="Fill" Value="{DynamicResource Brush.Selection}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckedFill" Property="Visibility" Value="Visible"/>
                <Setter TargetName="Dot" Property="Visibility" Value="Visible"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="CheckedFill" Property="Fill" Value="{DynamicResource Brush.Action.Hover}"/>
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Ring" Property="Fill" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="Ring" Property="Stroke" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter TargetName="CheckedFill" Property="Fill" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="Dot" Property="Fill" Value="{DynamicResource Brush.Disabled.Foreground}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="RadioButton.Default" TargetType="{x:Type RadioButton}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FocusVisual.Small}"/>
        <Setter Property="Background" Value="{DynamicResource Brush.Background.Surface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border.Input}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Thickness.Border}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template" Value="{StaticResource RadioButton.Template}"/>
    </Style>
    <Style TargetType="{x:Type RadioButton}" BasedOn="{StaticResource RadioButton.Default}"/>

</ResourceDictionary>
