<!--
    FAKT · Arctic Blue · navigation (DESIGN.md §5.1).

    Navigation.Item - a RadioButton in the side rail (one group per rail):
        <RadioButton Style="{StaticResource Navigation.Item}" ui:Ui.Icon="{StaticResource Icon.FolderOpen}"
                     Content="Обработка" IsChecked="{Binding …}"/>
    Unchecked: transparent, Navy text/icon; hover Brush.Selection (Blue.200, fades 120 ms);
    checked: Brush.Action.Primary with white text and icon (hover: Action.Hover).
    Compact rail (window < 1280 DIP): set ui:Ui.IsCompact="True" on the rail container - the label
    is hidden, the icon centred and the Content is shown as a tooltip (and remains the automation name).
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:Fakt.Desktop.Controls">

    <!-- Token dictionaries referenced with StaticResource by this file (StaticResource does not
         see sibling merged dictionaries, so every dictionary merges what it needs itself). -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Buttons.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="Navigation.Item.Template" TargetType="{x:Type RadioButton}">
        <Grid x:Name="Root" Background="Transparent" SnapsToDevicePixels="True" UseLayoutRounding="True">
            <Border x:Name="Base"
                    Background="{TemplateBinding Background}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="HoverLayer"
                    Opacity="0"
                    Background="{DynamicResource Brush.Selection}"
                    CornerRadius="{StaticResource Radius.Control}"/>
            <Border x:Name="CheckedLayer"
                    Visibility="Collapsed"
                    Background="{DynamicResource Brush.Action.Primary}"
                    CornerRadius="{StaticResource Radius.Control}"/>

            <Grid x:Name="ContentHost"
                  Margin="{TemplateBinding Padding}"
                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                  VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ui:Icon x:Name="Glyph"
                         Size="{StaticResource Size.Icon.Navigation}"
                         Data="{Binding Path=(ui:Ui.Icon), RelativeSource={RelativeSource TemplatedParent}}"/>
                <ContentPresenter x:Name="Label"
                                  Grid.Column="1"
                                  Margin="12,0,0,0"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                    <ContentPresenter.Resources>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </ContentPresenter.Resources>
                </ContentPresenter>
            </Grid>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="ui:Ui.Icon" Value="{x:Null}">
                <Setter TargetName="Glyph" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="Label" Property="Margin" Value="0"/>
            </Trigger>
            <Trigger Property="ui:Ui.IsCompact" Value="True">
                <Setter TargetName="Label" Property="Visibility" Value="Collapsed"/>
                <Setter TargetName="ContentHost" Property="HorizontalAlignment" Value="Center"/>
                <Setter TargetName="ContentHost" Property="Margin" Value="0"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsEnabled" Value="True"/>
                </MultiTrigger.Conditions>
                <MultiTrigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverLayer" Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.EnterActions>
                <MultiTrigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverLayer" Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="{StaticResource Duration.Hover}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </MultiTrigger.ExitActions>
            </MultiTrigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckedLayer" Property="Visibility" Value="Visible"/>
                <Setter TargetName="ContentHost" Property="TextElement.Foreground" Value="{DynamicResource Brush.Text.OnAction}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsChecked" Value="True"/>
                    <Condition Property="IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="CheckedLayer" Property="Background" Value="{DynamicResource Brush.Action.Hover}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsChecked" Value="True"/>
                    <Condition Property="IsPressed" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="CheckedLayer" Property="Background" Value="{DynamicResource Brush.Action.Pressed}"/>
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="CheckedLayer" Property="Background" Value="{DynamicResource Brush.Disabled.Background}"/>
                <Setter TargetName="ContentHost" Property="TextElement.Foreground" Value="{DynamicResource Brush.Disabled.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="Navigation.Item" TargetType="{x:Type RadioButton}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="FocusVisualStyle" Value="{DynamicResource FocusVisual.Control}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Font.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSize.Body}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="MinHeight" Value="{StaticResource Size.Nav.Item}"/>
        <Setter Property="Padding" Value="{StaticResource Thickness.Nav.Padding}"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="ToolTipService.Placement" Value="Right"/>
        <Setter Property="ToolTipService.InitialShowDelay" Value="300"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template" Value="{StaticResource Navigation.Item.Template}"/>
        <Style.Triggers>
            <!-- Compact rail: the label becomes the tooltip. -->
            <Trigger Property="ui:Ui.IsCompact" Value="True">
                <Setter Property="ToolTip" Value="{Binding Content, RelativeSource={RelativeSource Self}}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        Header actions («Администрирование», «Справка»): ghost button with icon and regular-weight text.
        Icon-only header actions should use Button.Icon with ToolTip + AutomationProperties.Name.
    -->
    <Style x:Key="Navigation.HeaderButton" TargetType="{x:Type ButtonBase}" BasedOn="{StaticResource Button.Ghost}">
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Padding" Value="12,0"/>
    </Style>

</ResourceDictionary>
